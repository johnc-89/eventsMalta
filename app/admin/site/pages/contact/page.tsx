'use client'

import BlockBuilder from '../../blocks/_components/BlockBuilder'

export default function ContactPageEditor() {
  return <BlockBuilder slug="contact" allowImportFromSections={false} />
}
